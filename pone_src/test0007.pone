define add ass bss : Product a a
where (a < Monoid) as
    define a1 : a as (add (fst ass) (snd ass)) in
    define b1 : a as (add (fst bss) (fst bss)) in
    Product a1 b1
end
0
;0
;type with constraints